
Implement the following three features:


1) remove the capability to load the variables for the LLM connection like api key etc from environment variabies or .env file. It should be done exclusuively through the settings functionality (system or app setting)

2) The model combobox at the bottom of the chatbox should be filled witht the elements from the available_models settings variable 

3) The connection should be made with the variables from the settings file and the chosen model 

